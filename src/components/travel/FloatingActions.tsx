import { useEffect, useState } from "react";
import { ArrowUp, Share2, Printer } from "lucide-react";
import { toast } from "sonner";
const summary=`✈️ Europa até Liverpool 2027\n\n🇮🇹 Itália · 🇨🇭 Alpes · 🇬🇧 30 dias em Liverpool\n📚 Intercâmbio + roteiro das amigas\n🥋 Jiu-jitsu em Liverpool\n\nPlanejamento completo no link 👇`;
export const FloatingActions=()=>{const[show,setShow]=useState(false);useEffect(()=>{const f=()=>setShow(window.scrollY>600);window.addEventListener("scroll",f);return()=>window.removeEventListener("scroll",f)},[]);const share=()=>{const text=`${summary}\n${window.location.href}`;if(navigator.share)navigator.share({title:"Europa até Liverpool 2027",text}).catch(()=>{});else navigator.clipboard.writeText(text).then(()=>toast.success("Resumo copiado"));};return <div className="fixed bottom-5 right-5 z-50 flex flex-col gap-3 no-print"><button onClick={share} className="h-12 w-12 rounded-full bg-olive text-primary-foreground shadow-soft grid place-items-center" aria-label="Compartilhar"><Share2 className="h-5 w-5"/></button><button onClick={()=>window.print()} className="h-12 w-12 rounded-full bg-card border border-border text-ink shadow-card grid place-items-center" aria-label="Imprimir"><Printer className="h-5 w-5"/></button><button onClick={()=>window.scrollTo({top:0,behavior:"smooth"})} className={`h-12 w-12 rounded-full bg-gold text-ink shadow-soft grid place-items-center transition-all ${show?"opacity-100":"opacity-0 pointer-events-none"}`} aria-label="Topo"><ArrowUp className="h-5 w-5"/></button></div>};